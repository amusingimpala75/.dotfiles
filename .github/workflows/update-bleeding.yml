name: Flake bleeding update

'on':
  workflow_dispatch: { }
  schedule:
    - cron: "0 8 * * 1"

jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
    - uses: actions/checkout@v6

    - uses: cachix/install-nix-action@v31
      with:
        extra_nix_config: experimental-features = flakes nix-command pipe-operators

    - name: Update flake
      run: nix develop -c just update-bleeding

    - name: Verify update
      run: nix -Lv flake check --build-all

    - name: Commit update
      uses: stefanzweifel/git-auto-commit-action@v7
      with:
        commit_message: "flake: update bleeding inputs"
        branch: master
        commit_options: '--no-verify --signoff'
        file_pattern: flake.lock
        commit_user_name: Flake Bot
        commit_author: Flake Bot <actions@github.com>
        skip_dirty_check: false
        skip_fetch: true
